# 2026-07-17 当日工作清单（CSMS v5）

> 本文件夹汇总当天「生成的东西 + 改动的代码」，便于多台电脑按天同步。
> **同步方法**：把这整个 `2026-07-17/` 文件夹复制到另一台电脑的同路径（`项目根/deliverables/2026-07-17/`）即可，工作无缝接上。

## 一、生成的文档（docs）
| 文件 | 内容 | 用途 |
|------|------|------|
| `UX-RESEARCH-2026-07-17.md` | 用户体验研究报告（可用性测试、严重度、用户画像、情绪旅程） | 决策依据 |
| `UX-DECISIONS-TASKS-2026-07-17.md` | UX 决策定稿 + 22 条开发任务（含 AC） | 开发清单 |
| `OPTIMIZATION-MASTER-PLAN-2026-07-17.md` | 全局优化主计划（M1–M6 + 优先级 + 风险） | 总纲 |
| `ARCH-ROADMAP-2026-07-17.md` | 架构演进评审（认证/后台缺口、未来路线、E1–E5） | 架构决策 |
| `ARCH-MOBILE-WECHAT-2026-07-17.md` | 手机/微信端架构附录（H5 vs 小程序、D-W1/2/3） | 移动端决策 |
| `PROGRAM-ROLLUP-PLAN-2026-07-17.md` | 整合实施铺排总纲（Program、4 批次、RACI、效果指标） | 交付总控 |
| `DECISION-MENU-2026-07-17.md` | 待拍板决策清单（8 项选择题 + 优缺点） | 拍板用 |
| `SYNC-GUIDE.md` | **跨机同步操作指南**（v3 单文件聊天框方案） | 本文件夹 |
| `SYNC-2026-07-17.md` | **单文件同步包**（发聊天框用，含 2 记忆+1 用户级+9 文档） | 跨机同步 |
| `backlog-plan-2026-07-17.md` | 原有 backlog 计划（同日生成） | 参考 |

## 二、改动的代码（changes）
**今天无源代码改动** —— 全天为分析/规划类产出，未修改 `client/` 或 `server/` 任何文件。
> 今后若有代码改动，被改的文件会复制进本文件夹的 `changes/` 子目录（保留相对路径，如 `changes/client/src/...`），并在此处逐条列出，方便你覆盖到另一台电脑。

## 三、跨机同步（单文件聊天框方案 v3，已生成 ✅）
> 今天已用 `scripts/sync-package-single.cjs 2026-07-17` 生成 **`SYNC-2026-07-17.md`**（单个文件，97KB，含 12 块：2 记忆 + 1 用户级 + 9 文档）。
> **vc 真实工作流是"把文件发到另一台电脑的 WorkBuddy 聊天框"——聊天框发不了文件夹，故改用单文件包。**
- **怎么用**：把 `SYNC-2026-07-17.md` 这一个文件发到 B 电脑 CSMS 项目聊天框 → 对 B 的 WorkBuddy 说"请按同步包还原" → 自动写回记忆与文档。无需拷文件夹、无需脚本。
- 同步包用分隔符 `§§F|<相对路径>|§§` / `§§U|§§` 序列化；首行是给 WorkBuddy 的执行协议注释。
- ❌ 凭证 `SECRETS.md` 永不进包（每台电脑各自维护）
- **双向对称**：B 收尾也会自动生成单文件包，回传同样只发一个 `.md` 文件
- 详见同文件夹 `SYNC-GUIDE.md`（v3）

## 四、决策锁定状态（2026-07-17 23:19 更新）
- **8/8 开放决策已全部拍板锁定**（E1–E5 + D-W1/2/3），设计冻结。
- **E5 升级为 rev2**：vc 以集团化 CFO+公司秘书视角，将"4 角色 + 服务端矩阵"具象为 **5 角色（system_admin / group_admin / company_secretary / auditor / member）+ 动态 Scope（accessibleCompanies 行级权限）+ 可视化权限矩阵 + 全量审计日志（举证）**。
- E2 由 vc 具象为"System Admin → Group Admin → 邮件邀请 → 预置权限"标准运营流程，与 E2-A 100% 兼容。
- 文件 `DECISION-MENU-2026-07-17.md` 已改写为"已锁定"版本，含完整角色表、行级权限 schema、Wave 0 三项增量（Personnel.accessibleCompanies / 全局 Query Filter 中间件 / 后台分配页）。
- 下一步：按 SOP 拉起 **Wave 0（地基与安全）**，实现项见《PROGRAM-ROLLUP-PLAN-2026-07-17.md》+ 本文件 E5 rev2。

## 五、使用约定（长期）
- 每天新建一个 `deliverables/YYYY-MM-DD/` 文件夹；
- 当天生成的所有文档放进该文件夹根；
- 当天改动的任何源码，复制进 `changes/`（保留项目内相对路径）；
- 写一份 `CHANGELOG.md` 说明内容与同步方法；
- **跨机同步 = 单个 `.md` 同步包（适配聊天框发文件）**：每次收尾 AI 自动 `node scripts/sync-package-single.cjs <today>` 生成 `SYNC-<date>.md`（含所有记忆+文档）；**发到另一台电脑聊天框** → 说"请按同步包还原"即自动展开。A↔B 双向只发一个文件。凭据 `SECRETS.md` 不进包。详见 `SYNC-GUIDE.md` v3。
